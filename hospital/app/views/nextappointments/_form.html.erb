<%= form_for(@nextappointment) do |f| %>
  <% if @nextappointment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nextappointment.errors.count, "error") %> prohibited this nextappointment from being saved:</h2>

      <ul>
      <% @nextappointment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date_of_next_appointment %><br>
    <%= f.text_field :date_of_next_appointment %>
  </div>
  <div class="field">
    <%= f.label :reason_for_visit %><br>
    <%= f.text_field :reason_for_visit %>
  </div>
  <div class="field">
    <%= f.label :appointment_id %><br>
    <%= f.text_field :appointment_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
